import React from "react";

const NotFoundRoute = () => {
  return (
      <h1  style={{marginTop: "5rem", textAlign: "center"}}>404 Error, page doesn't exist.</h1>
  );
};

export default NotFoundRoute;